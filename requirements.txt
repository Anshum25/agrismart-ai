# Training + model export only (run on Google Colab).
# The deployed API does NOT need TensorFlow: see backend/requirements.txt.
tensorflow>=2.15,<2.18
scikit-learn>=1.3
numpy>=1.24,<2.0
pillow>=10.0
opencv-python-headless>=4.8
kaggle>=1.6

# ONNX export + quantization + parity check
tf2onnx>=1.16
onnx>=1.15
onnxruntime>=1.17
